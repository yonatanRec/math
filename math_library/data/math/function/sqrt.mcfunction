#math:sqrt

### Input:
# in: the number for calculating
# out: storage are for result

# Equivalent to `√x`

### Output: 
# storing final value into `out`






$return run function sqrt:calc {x:$(in), out:"$(out)", eps:0.01}