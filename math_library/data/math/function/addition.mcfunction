# just a director to the actual function (addition:calc)

### input:
# a: any number, can be positive or negative, can have a decimal part
# b: a second number to add to the first number, can also be positive or negative, can also have a decimal part
# out: storage location for result

$return run function addition:calc {a:$(a), b:$(b), out:"$(out)"}